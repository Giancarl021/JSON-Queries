{
  "name": "json-queries",
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "post-build": "node scripts/post-build.js",
    "gh-pages": "gh-pages -d dist",
    "deploy": "vite build && node scripts/post-build.js && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^3.1.0",
    "vite": "^2.1.5"
  },
  "dependencies": {
    "alasql": "^1.7.2",
    "jmespath": "^0.15.0",
    "jsonpath": "^1.1.1",
    "sass": "^1.32.10",
    "sweetalert2": "^10.16.3"
  }
}
